# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration (Optional - server runs without it)
# Uncomment when you have MongoDB set up:
# MONGODB_URI=mongodb://localhost:27017/joinme
# Or for MongoDB Atlas: MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/joinme

# JWT Secret (for future authentication)
JWT_SECRET=your_jwt_secret_key_here

# CORS Origin (for frontend)
CORS_ORIGIN=http://localhost:3000
